执行以下步骤，不要做其他任何事情，不要访问学校网站做大规模探测（只对日志中点名的问题域名做一次性 curl 复测）：

第1步：用 read_file 读 /home/university-notice-collector/logs/cron-crawl.log 的最后 250 行，和最近的 inspection-*.txt 文件（用 search_files 找到）。
第2步：从日志中找出问题域名（抓取失败/NameResolutionError/ConnectionError/超时等涉及的 URL）。
第3步【复测甄别】：对第2步找出的每个问题域名，在服务器上用 curl 命令复测一次连通性（如 `curl -s --noproxy '*' -m 6 -o /dev/null -w '%{http_code}' https://域名/`），并据此把问题分类：
  - 复测 200/302/412 → 「网络抖动误报或反爬」，无需改域名（412 可建议加 browser 标记）
  - 复测 000 且 DNS 无法解析（NXDOMAIN）→ 「真域名错误」，进入第 3.5 步
  - 注意：不要仅凭日志就断言域名失效，必须以复测结果为准。
第3.5步【搜索核实】：对确认「真域名错误」的学校，用 web 搜索工具查该校研究生院/招生网的真实官方域名（如搜“XX大学研究生院 官网”），并把查到的候选域名用 curl 复测验证连通性（200 才算数）。把验证通过的真实域名作为替代建议写进报告。
第4步：用中文写复盘报告，分三节：1)致命问题（注明复测结果与替代域名） 2)待观察 3)具体修复建议（每条指明改哪个栏目/URL，区分"建议改域名（给出已核实替代）"和"误报不用动"）。要点到为止，不要长篇。
第5步：用 write_file 把报告写入 /home/university-notice-collector/logs/review-当前日期.md（今天是 2026-09-09）。写完后立即结束。

报告模板：
## 复盘总结 日期（对应采集时间）
### 致命问题（已复测确认）
### 待观察（可能误报/需观察）
### 具体修复建议
