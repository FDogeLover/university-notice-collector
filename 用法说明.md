# 大学信息收集 - 使用说明

把指定高校官网发布的通知/信息采集进本地数据库（每条带官方原文链接 + 快照），
需要时通过关键词/提问检索整合，保证**可溯源、不编造**。

## 一、项目结构

```
大学信息收集/
├── config/
│   └── schools.yaml     # 学校 + 栏目入口清单（增删改学校就在这里）
├── crawler/
│   ├── fetch.py         # 采集器：下载页面
│   ├── parse.py         # 解析器：抽链接 / 详情 / 类型打标
│   └── dedup.py         # 去重：URL + 标题指纹
├── db/
│   ├── schema.sql       # 建表语句
│   └── store.py         # 入库 / 查询
├── data/
│   └── university.db    # SQLite 数据库（首次运行时自动生成）
├── run.py               # 手动触发采集
└── query.py             # 检索工具
```

## 二、运行环境

- Python 3.10+
- 依赖：`pip install requests beautifulsoup4 pyyaml`

## 三、使用步骤

### 1. 采集（手动触发）

```bash
# 查看已配置的学校/栏目入口（首次运行会自动建库）
python run.py --list

# 采集全部学校
python run.py

# 只采集指定学校 / 指定栏目
python run.py --school 深圳大学
python run.py --school 深圳大学 --source 研究生招生网

# 只存标题+原文链接，不抓详情页（更快）
python run.py --no-detail

# 详情页并发抓取（默认 6 线程；浏览器反爬栏目自动保持串行）
python run.py --workers 8
```

采集结果会打印新增通知，并自动写入 `data/university.db`。
重复运行不会重复入库（按原文链接 + 标题指纹去重）。

### 2. 检索

```bash
# 关键词搜索（标题+正文）
python query.py --keyword 推免
python query.py --keyword 推免 --school 深圳大学

# 看最近入库
python query.py --recent --limit 30
```

每条结果都带**官方原文链接**，可点击核对。

### 3. 智能整合

直接问我即可，例如：
- “深圳大学 2027 推免预报名通知有哪些？截止日期是什么时候？”
- “这几所学校里，招推免生的计算机相关学院有哪些？”
我会先查数据库，整合成带原文链接的答案；库里没有的会明确说“未找到官方来源”，不会编造。

## 四、新增/修改学校

编辑 `config/schools.yaml`，在 `schools` 下追加一项：

```yaml
- name: 某大学
  domain: xxx.edu.cn
  sources:
    - name: 研究生招生网
      url: https://yz.xxx.edu.cn/
      category: 招生
    - name: 信息公开网
      url: https://xxgk.xxx.edu.cn/
      category: 信息公开
```

然后重新运行 `python run.py` 即可。

## 五、数据表说明

| 表 | 作用 |
|---|---|
| schools | 学校名单、域名 |
| sources | 每校采集入口（栏目） |
| notices | 通知本体：标题、正文快照、**官方原文链接**、发布时间、类型标签 |
| notice_meta | 结构化字段（截止日期 deadline/deadline_iso、学院、对象等，采集与回填时自动提取） |
| fetch_logs | 每次抓取的状态与新增数 |

## 六、Web 操作界面

启动（项目根目录执行）：
    python web/app.py
浏览器打开 http://127.0.0.1:8000/

功能：统计概览（学校/通知数）、按学校/类型/关键词筛选、近7天/30天快筛、
即将截止提醒、点卡片看正文快照、点“官方原文”跳到学校官网核对、
顶部“触发采集”按钮一键采集（后台运行）、“管理学校”停用/启用/删除、
“添加学校”支持 AI 智能填写。

## 七、静态展示站（发布给他人看）

不想自己跑 Python 的用户，可以把采集成果导出成一个**纯展示静态网站**：

    python build_site.py              # 输出到 site/ 目录

- 无 AI、无采集按钮，只负责展示：统计、筛选、关键词搜索、即将截止、正文快照、官方原文链接
- `site/` 目录**双击 index.html 就能打开**（不依赖服务器）；
- 部署：把 `site/` 整个目录上传到 GitHub Pages、Netlify、服务器等任意静态主机，
  绑定你的域名即可；全部相对路径，放在域名子路径下也能用；
- 数据有更新后重新执行 `python build_site.py` 覆盖 `site/` 即可；
- 停用学校不会出现在静态站里。

## 八、注意事项

- 采集频率建议手动/低频率，避免给学校服务器造成压力。
- 若某校官网启用了反爬，可调整 `crawler/fetch.py` 中的请求头或抓取间隔。
- 数据库文件可用环境变量 `UNIV_DB` 覆盖到其它路径。
